<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="LightButtonStyle">
        <item name="android:backgroundTint">@color/light_buttonColor</item>
        <item name="android:textColor">@color/buttonTextColor</item>
        <item name="android:padding">8dp</item>
        <item name="android:minHeight">48dp</item> <!-- Altura mínima do botão -->
        <item name="android:minWidth">20dp</item> <!-- Largura mínima do botão -->
        <item name="android:layout_margin">8dp</item>
    </style>
    <style name="DarkButtonStyle">
        <item name="android:backgroundTint">@color/dark_buttonColor</item>
        <item name="android:textColor">@color/buttonTextColor</item>
        <item name="android:padding">8dp</item>
        <item name="android:minHeight">48dp</item> <!-- Altura mínima do botão -->
        <item name="android:minWidth">20dp</item> <!-- Largura mínima do botão -->
        <item name="android:layout_margin">8dp</item>
    </style>
    <style name="AnotherButtonStyle">
        <item name="android:backgroundTint">@color/another_buttonColor</item>
        <item name="android:textColor">@color/buttonTextColor</item>
        <item name="android:padding">8dp</item>
        <item name="android:minHeight">48dp</item> <!-- Altura mínima do botão -->
        <item name="android:minWidth">20dp</item> <!-- Largura mínima do botão -->
        <item name="android:layout_margin">8dp</item>
    </style>
</resources>
